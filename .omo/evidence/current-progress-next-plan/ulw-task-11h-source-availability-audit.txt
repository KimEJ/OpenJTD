# G009 C001 PageMark/LineMark semantic source availability audit
date=2026-07-02T03:04:26Z
question=Does any available local spec, clean-room third-party artifact, or public web result supply independent PageMark/LineMark field semantics sufficient for tsaiten source-only table-family page-y admission?
answer=no-safe-source-backed-implementation-target-found

## local RFC 0007 PageMark evidence
    34	## PageMark Observation
    35	
    36	The first 12 bytes look like a compact header in the observed large samples. Raw `u32be` diagnostics are available through `stream-dwords` and `stream-dword-frequencies`.
    37	
    38	```text
    39	46.jtd  00000060 00000010 0000005f
    40	a5.jtd  0000004a 00000010 00000049
    41	b6.jtd  00000062 00000010 00000061
    42	```
    43	
    44	Working interpretation:
    45	
    46	- first `u32be`: count-like value;
    47	- second `u32be`: `0x10`, likely a size or stride-like value;
    48	- third `u32be`: first value minus one;
    49	- the next `u32be` is row 0's index-like value in the observed 84-byte row family.
    50	
    51	The total stream size does not reduce to a simple `header + count * 0x10` formula. However, the current large samples prove one stable family:
    52	
    53	```text
    54	12-byte header
    55	N rows of 84 raw bytes
    56	```
    57	
    58	The first dword in each 84-byte row is an index-like value. The rest of the row is preserved as raw bytes because the fields are not semantically decoded yet.
    59	
    60	`rjtd page-marks <file>` exposes the row parser with two decoded diagnostic fields derived from offsets in the raw row bytes:
    61	
    62	- `lineStart`: a u16 at a fixed offset within the row, interpreted as a layout line start position.
    63	- `lineEnd`: a u16 at a fixed offset within the row, interpreted as a layout line end position.
    64	- `flags`: a u32 at a fixed offset.
    65	- `u16Class`: a heuristic label derived from the `lineStart`/`lineEnd` pattern — `additive-boundary` for rows where both values are small and `lineEnd > lineStart` with regular spacing; `mixed-payload` for rows where both values are anomalously large or internally inconsistent; `zero-sentinel` for rows where `lineStart=lineEnd`.
    66	
    67	Analysis of the 11 government/academic samples reveals that `lineStart`/`lineEnd` encode **layout row positions (0-based)**. The span `lineEnd − lineStart` is constant within a file's dominant entry family and equals `rows_per_page − 1`:
    68	
    69	| Sample | dominant span | rows per page |
    70	| --- | ---: | ---: |
    71	| `論文様式.jtd` | 43 | 44 (45-row-per-page template, last line = 43) |
    72	| `01要綱（事務局組織令）.jtd` | 12 | 13 |
    73	| `02案文・理由（整備令）.jtd` | 12 | 13 |
    74	| `04参照条文（施行日政令）.jtd` | 29 | 30 |
    75	| `04参照条文（整備政令）.jtd` | 29 | 30 |
    76	
    77	Consecutive `additive-boundary` entries step forward by exactly `rows_per_page` in `lineStart` — i.e. they tile the layout coordinate space in fixed page-height increments. `mixed-payload` entries at the same index carry anomalously large or internally inconsistent values and likely represent style-record slots, not body pages. `zero-sentinel` entries have `lineStart = lineEnd` and appear at the end of a group as spacer or sentinel positions. The total number of normal `additive-boundary` entries far exceeds the visible document content in short samples (e.g. `01要綱（事務局組織令）` has 130+ additive entries but only 9 text lines), suggesting that PageMark pre-allocates a fixed layout-slot grid regardless of actual content length. The physical meaning of the layout row coordinates — in particular whether row 0 is the first printable line of the document body or includes margins and headers — is not decoded.
    78	
    79	The `count_value = last_index_value + 1` invariant holds for all 17 tested samples (Ginga large x3 + 14 local government/academic). `/PageMark` and `/PaperMark` share the same `count_value` in every sample. The meaning of `count_value` is not decoded.
    80	

## local RFC 0007 LineMark evidence
   259	## LineMark Observation
   260	
   261	`/LineMark` begins differently and looks closer to a token/control stream than a simple fixed-width numeric table:
   262	
   263	```text
   264	a5.jtd  0914 0000 0001 0000 048f 0000 0003 0000
   265	46.jtd  0914 0000 0001 0000 050e 0000 0015 0000
   266	b6.jtd  0914 0000 0001 0000 0531 0000 0002 0000
   267	```
   268	
   269	After the first words, common values such as `000d`, `000a`, `0011`, `0019`, `0082`, and `1002` recur. These overlap with values already seen around `/DocumentText` control and inline segments, so `/LineMark` should be investigated together with the structured `DocumentText` token map.
   270	
   271	`stream-words` shows the first words as:
   272	
   273	| Sample | word 0 | word 4 | word 8 |
   274	| --- | --- | --- | --- |
   275	| `46.jtd` | `0x0914` | `0x050e` | `0x050d` |
   276	| `a5.jtd` | `0x0914` | `0x048f` | `0x048e` |
   277	| `b6.jtd` | `0x0914` | `0x0531` | `0x0530` |
   278	
   279	Working interpretation:
   280	
   281	- word 0 is a `LineMark` header/type candidate;
   282	- word 4 is count-like;
   283	- word 8 is word 4 minus one;
   284	- words 6 differs by sample and is not decoded.
   285	
   286	Raw word-frequency comparison shows that many small `/LineMark` words also appear in raw `/DocumentText`, but the high-frequency `0x1000`, `0x1001`, and `0x1002` words appear to be `/LineMark`-specific tags:
   287	
   288	| Sample | `0x1000` | `0x1001` | `0x1002` |
   289	| --- | ---: | ---: | ---: |
   290	| `46.jtd` | 302 | 24 | 165 |
   291	| `a5.jtd` | 293 | 18 | 200 |
   292	| `b6.jtd` | 308 | 25 | 169 |
   293	
   294	These tag-like values are not treated as `DocumentText` control codes.
   295	
   296	`rjtd line-mark-tags <file>` scans `/LineMark` for these tag-like words and prints their word index, byte offset, previous four words, and next six words. A sample-wide sweep across the current 61 local samples produced:
   297	
   298	| Group | Count |
   299	| --- | ---: |
   300	| files with tag rows | 5 |
   301	| files without `/LineMark` | 6 |
   302	| readable `/LineMark` streams with no tag rows | 50 |
   303	| total tag rows | 1536 |
   304	| `0x1000` rows | 915 |
   305	| `0x1001` rows | 67 |
   306	| `0x1002` rows | 554 |
   307	
   308	The five files with tag rows are:
   309	
   310	| Sample | `0x1000` | `0x1001` | `0x1002` | total |
   311	| --- | ---: | ---: | ---: | ---: |
   312	| `46.jtd` | 302 | 24 | 165 | 491 |
   313	| `a5.jtd` | 293 | 18 | 200 | 511 |
   314	| `b6.jtd` | 308 | 25 | 169 | 502 |
   315	| `ichitaro-20030316043238-success-001-success_data-iwata_file.jtd` | 0 | 0 | 5 | 5 |
   316	| `ichitaro-20030706234132-success-004-success_data-asobinin_24.jtd` | 12 | 0 | 15 | 27 |
   317	
   318	The first word after a tag is not a unique family discriminator. Frequent next words such as `0x0025`, `0x0027`, `0x002d`, `0x004d`, `0x0037`, and `0x0035` overlap across tag families, so the next word is currently treated as payload-like context rather than a decoded tag subtype.
   319	
   320	`rjtd line-mark-text-context <file>` compares each tag row with the `/DocumentText` token map. It tests two weak hypotheses: whether the LineMark word/byte offset directly lands in a `DocumentText` map entry, and whether the immediate next word appears anywhere in raw `/DocumentText`.
   321	
   322	Across the same 61 local samples:
   323	
   324	| Metric | Count |
   325	| --- | ---: |
   326	| files checked successfully | 55 |
   327	| files without `/LineMark` | 6 |
   328	| tag rows reported | 1536 |
   329	| direct LineMark byte-offset hits in `DocumentText` map | 587 |
   330	| direct LineMark unit-offset hits in `DocumentText` map | 587 |
   331	| tag rows whose immediate next word appears in raw `/DocumentText` | 1511 |
   332	
   333	Per tag family:
   334	
   335	| Tag | direct byte hits | direct unit hits | next-word raw `DocumentText` hit rows |
   336	| --- | ---: | ---: | ---: |
   337	| `0x1000` | 344 | 344 | 904 |
   338	| `0x1001` | 27 | 27 | 67 |
   339	| `0x1002` | 216 | 216 | 540 |
   340	
   341	Working interpretation: `/LineMark` tag-next words usually reuse values that occur somewhere in `/DocumentText`, but this does not make them direct offsets. Directly treating the LineMark tag index or byte offset as a `DocumentText` coordinate only hits 587 of 1536 rows, so `/LineMark` likely uses its own record coordinate system or layout-local payload fields.
   342	
   343	`rjtd text-position-line-context <file>` compares `MarkV.01` header/entry offsets against `/LineMark` word positions and nearest LineMark tag rows. Only three current samples expose both readable `/LineMark` and parsed `MarkV.01`: `46.jtd`, `a5.jtd`, and `b6.jtd`.
   344	
   345	| Sample | line words | line tags | Mark entries | Page rows | Paper rows | Mark header line index | LineMark word at header index | nearest tag rows |
   346	| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
   347	| `46.jtd` | 2597 | 491 | 8 | 97 | 97 | 1539 | `0x0029` | `0x1002@1520`, `0x1000@1564` |
   348	| `a5.jtd` | 2561 | 511 | 8 | 75 | 75 | 1539 | `0x0016` | `0x1002@1534`, `0x1002@1542` |
   349	| `b6.jtd` | 2667 | 502 | 8 | 98 | 98 | 1552 | `0x0000` | `0x1000@1548`, `0x1002@1554` |
   350	
   351	All 24 `MarkV.01` entry offsets in those three samples are outside the `/LineMark` word range. Therefore the entries are not direct LineMark word indexes. The final `u16` in the Mark header remains a LineMark-adjacent candidate because it lands inside `/LineMark` near tag clusters, but it is not decoded and does not land on a stable tag value.
   352	
   353	`rjtd text-position-count-layout-context <file>` extends the same direct-layout-coordinate check to `TCntV.01` tables. Across the 10 readable `TCntV.01` samples and 89 checked rows, the chosen range candidates produce 0 direct hits against `/LineMark` word offsets, `/LineMark` byte offsets, `/PageMark` rows/bytes, and `/PaperMark` rows/bytes. This means the current `TCntV.01` ranges also should not be treated as direct layout stream coordinates.
   354	
   355	`rjtd text-position-count-fields <file>` exposes the remaining `TCntV.01` tail as positional `u16be` fields, and `rjtd text-position-count-field-deltas <file>` compares the chosen range span with the tail `t1..t2` span. In the current samples, all 89 rows have `t2 >= t1`, but no row has `t2 - t1` equal to the chosen range span. `rjtd text-position-count-tail-context <file>` also shows that `t1/t2` has a stronger `/DocumentText` UTF-16 unit hit pattern than byte hit pattern, though it is not universal. `rjtd text-position-count-tail-delta-scan <file>` raises unit hits most around delta 29/30, but text hits peak elsewhere. `rjtd text-position-count-tail-delta-groups <file>` then splits that aggregate signal into tail-pattern groups: one major `be0` pattern prefers `+29`, shifted patterns prefer `+30/+31`, and a major `0x0202` pattern is spread across many best deltas. `rjtd text-position-count-tail-row-deltas <file>` confirms the major `0x0202` pattern remains spread across many row-level best deltas. `rjtd text-position-count-tail-row-context <file>` shows that `0x0202` chosen ranges often touch later body byte ranges while best-delta tail fields usually touch early heading/date text. `rjtd text-position-count-range-preview <file>` then shows that `0x0202` chosen byte ranges often overlap real `/DocumentText` text entries even though they are not direct layout-stream coordinates. `rjtd text-position-count-range-boundaries <file>` adds that the major `0x0202` byte ranges mostly contain whole `/DocumentText` map entries and repeatedly include `0x001c`/`0x000e` controls. `rjtd text-control-context <file>` then shows `0x001c` as a high-frequency delimiter candidate and `0x000e` as more control-cluster or inline-adjacent. This keeps `TCntV.01` tail fields promising and makes `/DocumentText` control-delimited byte intervals the next target for chosen-range analysis, but rejects treating `t1/t2` as a simple duplicate of the chosen range.

## local RFC 0007 known gaps and next steps
   445	## Known Gaps
   446	
   447	- No `LineMark` record parser exists yet.
   448	- `/PageMark` has raw-preserving parsers for fixed 84-byte rows, fixed 84-byte rows with preserved trailing bytes, count-plus-one variable-row families, and count-variable rows, but some local `/PageMark` streams still use unsupported variants.
   449	- `/PaperMark` has an initial row parser, but no semantic model mapping exists yet.
   450	- The count-like header values are not proven to be entry counts.
   451	- The relationship between these streams and `MarkV.01` / `TCntV.01` is not decoded; current evidence rejects direct Mark-entry-to-LineMark-word indexing and direct `TCntV.01` range-to-layout-stream offsets.
   452	- Small malformed samples can expose inventory entries whose mini-stream chains are not safely readable; `stream-meta` should be used before interpreting small layout streams.
   453	- `/LineMark` has tag-like values (`0x1000`, `0x1001`, `0x1002`) whose semantics are unknown; current tag-context evidence does not make the immediate next word a unique family discriminator or prove direct `DocumentText` coordinates.
   454	- The semantic meaning of the LineMark header word 0 (`0x0914` / `0x090b` / `0x0912`) is not decoded; it may encode document type, writing direction, or another document-level property.
   455	
   456	## Next Steps
   457	
   458	- Keep `/PaperMark` as a parser-backed diagnostic until the header and flag semantics are decoded.
   459	- Decode the `SO` object/control record family field semantics; current evidence suggests fields 1-4 carry geometry-like tuples in singleton records while repeated records carry default/control constants.
   460	- Compare `/PageMark` and `/PaperMark` count-like values with rendered page or paper counts in known samples.
   461	- Compare `/PaperMark` flags with page breaks, paper sections, and visible layout changes.
   462	- Decode the `0x1000`, `0x1001`, and `0x1002` tag families inside `/LineMark` by comparing tag offsets and contexts with text and layout boundaries.
   463	- Decode why the final Mark header `u16` lands inside `/LineMark` near tag clusters while the Mark entries do not.
   464	- Decode the actual target for `TCntV.01` ranges after direct `/LineMark`, `/PageMark`, and `/PaperMark` coordinates were rejected; use `text-position-count-fields`, `text-position-count-field-deltas`, `text-position-count-tail-context`, `text-position-count-tail-delta-scan`, `text-position-count-tail-delta-groups`, `text-position-count-tail-row-deltas`, `text-position-count-tail-row-context`, `text-position-count-range-preview`, `text-position-count-range-boundaries`, and `text-control-context` to compare tail field patterns and chosen `/DocumentText` byte-range/control overlap.

## local RFC 0006 layout-coordinate rejection evidence
   182	`rjtd text-boundary-candidate-layout-context <file>` compares unit-basis `0x001c` single candidates with direct `/LineMark`, `/PageMark`, and `/PaperMark` index/byte contexts:
   183	
   184	```text
   185	text-boundary-candidate-layout-context <file>
   186	summary unit-001c-single-candidates=<count> rule-selected=<count> line-bytes=<bytes> line-words=<words> page-rows=<rows> page-bytes=<bytes> paper-rows=<rows> paper-bytes=<bytes>
   187	candidate <index> range=<textCountRangeIndex> selected=<bool> edge-good=<bool> non-empty=<bool> line-breaks=<count> source=<unit-start-end> text=<preview> line-word-start=<context> ... paper-byte-end=<context> decoded=false
   188	```
   189	
   190	The current layout-context sweep finds 52 unit `0x001c` single candidates across 8 files and 35 strict rule-selected rows. None of the selected rows has start/end direct hits in `/LineMark`, `/PageMark`, or `/PaperMark`. Therefore candidate source units and layout mark rows are not the same coordinate space; paragraph promotion still needs a separate layout-mark mapping rule.
   191	
   192	`rjtd text-boundary-layout-map <file>` scores the same unit-basis `0x001c` candidates against sparse layout point sets using several global source-unit transforms:
   193	
   194	```text
   195	text-boundary-layout-map <file>
   196	summary unit-001c-single-candidates=<count> rule-selected=<count> target-sets=<count> bases=<count> delta-range=<min>..<max>
   197	best scope=<all|selected> target=<point-set> base=<unit-transform> delta=<signed-delta> delta-at-boundary=<bool> points=<count> candidates=<count> endpoints=<count> valid=<count> invalid=<count> exact=<count> total-distance=<sum|-> max-distance=<max|-> decoded=false
   198	```
   199	
   200	The current map sweep runs successfully on all 61 local samples. It finds the same 52 unit `0x001c` single candidates across 8 files and 35 strict selected candidates across 4 files. Non-boundary exact hits exist, but they do not converge on a single global transform: `iwata_file` favors `line-word-value` and `page-be32-field` with unit-div2 shifts around `-1140..-1192`, while the selected finance samples favor different `page-be32-field` shifts. Therefore paragraph promotion must look for row-local, section-local, or record-local base offsets instead of a file-global source-unit transform.
   201	
   202	`rjtd text-boundary-layout-map-rows <file>` scores each unit-basis `0x001c` candidate independently and includes the linked `TCntV.01` row summary:
   203	
   204	```text
   205	text-boundary-layout-map-rows <file>
   206	summary unit-001c-single-candidates=<count> rule-selected=<count> target-sets=<count> bases=<count> local-rows=<count>
   207	local candidate=<candidateIndex> range=<textCountRangeIndex> selected=<bool> target=<point-set> base=<unit-transform> delta=<signed-delta> delta-at-boundary=<bool> exact=<0..2> total-distance=<sum|-> max-distance=<max|-> start-nearest=<source:mapped->nearest:d> end-nearest=<source:mapped->nearest:d> source=<unit-start-end> text=<preview> tcnt=<row-summary> decoded=false
   208	```
   209	
   210	The row-local sweep also succeeds on all 61 local samples. It reports 52 unit `0x001c` single candidates and 35 strict selected candidates. Of the 32 strict selected candidates in `iwata_file`, 10 have row-local `exact=2` evidence through both `line-word-value` and `page-be32-field`. The three strict selected finance candidates have no row-local `exact=2` evidence. This means the strict edge/text rule is not sufficient by itself: the future paragraph rule needs a layout-local discriminator that keeps paragraph-like `iwata_file` rows separate from large spans.

## local TODO decoded-false blocker records
   279	- [x] Fully prove the record layout of `/PageMark` variants that are still unsupported by `page-marks`; RFC 0007 §PageMark Observation documents the three unsupported entries (`kaisya_annai`, `kazoku_ryoko`, `shanai_lan`) as stale/foreign payloads (CFB directory-entry fragments and OLE/ActiveX metadata) rather than layout rows — the miniFAT chains are complete but byte content is unrelated to PageMark row structure; no further proof is needed.
   280	- [x] Confirm from the reference `a5.pdf` that visible page numbers begin on page 6, even pages place page number and running chapter title on the left side, odd pages place page number and document title on the right side, and the running document title matches `/AutoTextInfo` text candidates.
   281	- [x] Expose `PageMark` first-field diagnostics as raw-preserving `flags`, `lineStart`, and `lineEnd` values so page rows can be compared against reference PDF pagination without claiming final semantics.
   282	- [x] Add `rjtd page-layout-style-slots <file>` to inspect `/PageLayoutStyle` nested `31xx..39xx` slot groups as decoded-false raw evidence, including active `paired-slot-pairs` and `facing-pages-candidate` summary fields for future facing-page/header/footer/page-number analysis.
   283	- [x] Check the local A/B/Ginga-style samples for facing-page evidence: `a5.jtd`, `a6.jtd`, `b6.jtd`, `46.jtd`, and `shinsyo.jtd` all expose active paired `/PageLayoutStyle` slot groups such as `0x32/0x33`, `0x34/0x35`, `0x36/0x37`, and `0x38/0x39`; page decoration layer output now explicitly marks `facingPagesCandidate:true` and `sidePolicyDecoded:false`.
   284	- [x] Preserve `/AutoTextInfo` UTF-16BE text candidates in the document model/export JSON so running document titles can be sourced from file data instead of hardcoded sample names.
   285	- [x] Render a decoded-false running header/page-number projection for vertical Ginga-style documents only when `/AutoTextInfo`, active paired `/PageLayoutStyle` slots, and document-text chapter-title evidence are all present; layer-tree output now carries `sidePolicy:"facing-pages-odd-right-even-left"` plus the source slot pairs, running headers render horizontally like the Ichitaro reference, and `openjtd-samples/pdf-output/a5.pdf` pages 6 and 7 were verified against the reference PDF. Page-number x placement now follows the page margin line: reference/generated page 6 x is about 57.4/54.0 pt, and page 7 x is about 358.8/360.7 pt.
   286	- [x] Remove the a5-only front-matter gate for the same Ginga vertical projection. With reference PDFs present, `a6.jtd` and `b6.jtd` now also expose page 6 as even-left with `一、午后の授業` and page 7 as odd-right with `銀河鉄道の夜`, while `getPageLayerTree` keeps `sidePolicyDecoded:false`, `facingPagesCandidate:true`, and the `0x32/0x33` through `0x38/0x39` paired slot evidence.
   287	- [x] Preserve the raw `/PageLayoutStyle` slot payload evidence behind decoded-false page decorations in `getPageLayerTree`: each `pageDecoration` now carries `slotEvidence` with record index/offset/label, slot id, `part05First`/`part05NonZero`, and opaque `part04..part07` hex payloads so future header/footer/page-number decoding can be traced without hardcoded geometry.
   288	- [x] Add `rjtd page-layer-tree <file> <zero-based-page-index>` so facing-page page-number/header projections can be inspected through the model layer tree, including `side`, `sidePolicyDecoded:false`, `facingPagesCandidate:true`, `pairedSlotPairs`, and `slotEvidence`.
   289	- [x] Fix `getPageLayerTree` JSON framing so the page-decoration evidence is machine-parseable; tests now check balanced JSON delimiters and the `root.ops` to `textSources` boundary.
   290	- [x] Preserve `DocumentText` table-of-contents page-label candidates from skipped-inline selector `0x0101` as model/export/app-core `tocEntries` with source spans and `decoded:false`; the A5 sample now exposes labels `6,10,12,16,21,23,28,35,42` without treating the selector as fully decoded.
   291	- [x] Render the A5 table-of-contents page from model-owned `tocEntries` instead of hardcoded labels, including leader dots, page labels, and source-span-backed ruby annotations for title text; `openjtd-samples/pdf-output/a5.pdf` page 3 was regenerated and checked against the reference PDF.
   292	- [x] Preserve `/PageMark` rows as decoded-false model/export/app-core `pageMarks`, including header count/stride/last-index, family, entry count, row index, flags, and `lineStart`/`lineEnd` fields. The A5 sample exposes `count=74`, `stride=16`, `last=73`, `75` parsed rows, and row 5 `lineStart=23,lineEnd=40`, which aligns with the reference body start page 6.
   293	- [x] Preserve `/PaperMark` rows as decoded-false model/export/app-core `paperMarks`, including header count/stride/last-index, entry count, row index, flags, flag hex, and raw row length. The A5 sample exposes `count=74`, `stride=12`, `last=73`, `75` parsed rows, and flags such as `0x00010010`/`0x00010011`; semantic page/paper meaning is still unclaimed.
   294	- [x] Attach decoded-false `/PageMark` + `/PaperMark` row-alignment evidence to page decoration layer-tree entries. A5 and B6 page 6 now expose matching `pageMarkEntryIndex=5` and `paperMarkEntryIndex=5`, aligned mark indexes, matching entry counts, and the source-backed `/PaperMark` flag (`0x00010010`) while blocking render promotion until flag semantics are decoded.
   295	- [x] Expose the same decoded-false page mark context through app-core `get_page_info` and CLI `rjtd page-info <file> <page>`. Mark-bearing pages report the source-backed `/PageMark+/PaperMark` row context, while markless fallback pages such as current `a6.jtd` report `layoutMarkEvidence:null`.
   296	- [x] Match the A5 generated PDF page count to the Ichitaro reference by tightening the vertical fallback display-unit advance instead of clamping page count. `openjtd-samples/pdf-output/a5.pdf` now has 72 pages with the same A5 portrait MediaBox as the reference.
   297	- [x] Project the A5/Ginga first body chapter title as a standalone vertical heading block instead of consuming it as a normal body column. Page 6 now ends at `大きな望遠鏡...何でしょう。`, and `やっぱり星だ...` starts on page 7 like the Ichitaro reference while keeping the 72-page count.
   298	- [x] Preserve `/Font` table names through model/export JSON and prefer Mincho-compatible aliases for PDF generation. On macOS, exporter font discovery now also loads Apple MobileAsset font directories so A5 embeds `YuMin-Medium` instead of falling back to `ArialUnicodeMS`.
   299	- [x] Guard PDF text-layer preservation in the local reference-backed export sweep: generated PDFs must retain `/ToUnicode` maps and CID font resources inside the svg2pdf Form XObject path, instead of silently degrading to path-only output.
   300	- [ ] Decode the exact semantics of `/PageLayoutStyle` nested `31xx..39xx` slot groups, including which active slot pairs correspond to facing pages, header/footer areas, and page-number fields. Cross-sample analysis of `part06` terminal u16 values across 10 samples (5 families): `sample-outline`/`sample-draft`→0x32/0x33=1000/0x34=453/0x36=600/0x38=407; `sample-table`→0x32/0x33=800/0x34=453/0x36=407+773/0x38=411; `sample-draft-b`→0x32/0x33=2000/all others=0; `sample-table-b` and `sample-table-c` are identical→0x32/0x33=1405/0x36=0+1397/all others=0. Part06 byte 1 is 0x01 only in `sample-outline`/`sample-draft`; 0x00 in all others. `sample-reference` variants and `sample-academic` have no `/PageLayoutStyle`. Correlation with physical page dimensions requires reference PDF calibration (blocked: a5.jtd absent).
   301	- [ ] Replace the current decoded-false running header/page-number projection with decoded layout records once source slots, page parity rules, chapter-title binding, and document-title binding are proven beyond the current a5-style evidence.
   302	- [ ] Decode the exact table-of-contents layout semantics behind the `0x0101` page-label candidate, including leader length, title/ruby binding, and page-label alignment; the current A5 projection is reference-backed but still decoded-false.
   303	- [ ] Replace the vertical fallback display-unit heuristic with a proven `/PageMark`/`/LineMark` mapping from original layout rows to `Document` text lines. Rows 73 and 74 look sentinel-like (`lineStart=lineEnd` with zero geometry), so they must not be blindly treated as visible pages. From 14 government/academic samples, `additive-boundary` PageMark entries have constant span `lineEnd−lineStart = rows_per_page−1` (e.g. span=29 for 30-row/page samples), meaning `lineStart`/`lineEnd` encode 0-based layout row positions — but the mapping from layout row to DocumentText unit coordinates is not decoded, and PageMark pre-allocates many more slots than the document contains text lines.

## clean-room OpenOffice Ichitaro filter metadata
   123	Plain string inspection found stream names that match local `.jtd` sample streams:
   124	
   125	```text
   126	DocumentText
   127	DocumentViewStyles
   128	Header
   129	PageLayoutStyle
   130	PageLayoutStyleHeader
   131	TextLayoutStyle
   132	JSRV_SegmentInformation
   133	```
   134	
   135	It also contains additional candidate stream or object names:
   136	
   137	```text
   138	LayoutBox
   139	LayoutBoxText
   140	Figure
   141	EmbedItems
   142	EmbeddingInfo
   143	Embedding
   144	Contents
   145	EmbeddedPress
   146	FigureData
   147	main_data
   148	FDMIndex
   149	FDMVector
   150	SsmgTextTcntQLST
   151	```
   208	## License Boundary
   209	
   210	The package includes a Sun software license. The license text restricts decompilation and reverse engineering unless enforcement is prohibited by applicable law.
   211	
   212	rjtd must treat this artifact as a no-code compatibility reference:
   213	
   214	- Do not copy implementation logic from the DLL.
   215	- Do not decompile the DLL as part of normal rjtd development.
   216	- Use extension metadata, filter registration, file inventory, and independently observable sample behavior as clean-room inputs.
   217	- Consult legal advice before any deeper binary analysis.

### filter binary string check, no decompilation
121:DocumentText
122:DocumentViewStyles
123:Header
124:PageLayoutStyle
125:PageLayoutStyleHeader
126:TextLayoutStyle
143:SsmgTextTcntQLST
380:Header

## public web search snapshot
searched=Ichitaro JTD PageMark LineMark file format; 一太郎 JTD PageMark LineMark 仕様; /LineMark DocumentText Ichitaro; /PageMark DocumentText JTD; SsmgV.01 LineMark; SsmgTextTcntQLST Ichitaro
result_1=https://github.com/KimEJ/OpenJTD and https://github.com/KimEJ/OpenJTD/blob/main/openjtd-spec/rfc/0007-layout-mark-streams.md were the only semantic-looking hits, matching this local repo/RFC rather than independent external semantics.
result_2=https://filext.com/file-extension/JTD and localized FILExt pages list JTD streams such as DocumentText/LineMark/PageMark/PaperMark but do not define field semantics or page-y transforms.
result_3=https://note.com/darkside_of_nhk/n/n28bbe1035636 lists DocumentText/PageMark as visible OLE streams in a practical conversion article but does not decode PageMark/LineMark fields.
result_4=JustSystems support/template pages describe application-level JTD/viewer/template behavior, not internal stream semantics.

## conclusion
sourceBackedImplementationTarget=false
decodedPageMarkSlotSemanticsFound=false
decodedLineMarkPageYTransformFound=false
cleanRoomThirdPartyUsableForThisGate=false
publicIndependentSemanticSpecFound=false
blocker=Available sources prove raw-preserving PageMark/LineMark inventories and several negative correlations, but no source maps tsaiten PageMark/LineMark fields to table-family page-y origin, table-top, row-delta, or exact source-gap-to-page-line transform.

## cleanup receipt
nl/sed/strings/rg and web search completed; no source files edited; no decompilation, tmux/browser/ports/temp dirs/subagents spawned.
