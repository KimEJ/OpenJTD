Final manual-QA supersession resolution

This file resolves the conservative gate-review concern that failed exploratory
artifacts remained in the manual-QA directory without an explicit supersession
record. The failed artifacts are retained for auditability; they are not the
final verdict artifacts.

Resolved exploratory failures:
- S003 failed because the first parser expected detailed render-readiness fields
  directly in the layer-tree JSON. Superseded by S004 and
  ../c002-render-admission-or-blocker.txt, which verify promotion_RED=false,
  projectionKind=diagnosticProjection, renderPromoted=false,
  sourceDerivedLayoutRenderable=false, and source-page-Y/source-gap blockers.
- S005 failed because pdfplumber was unavailable in that QA environment.
  Superseded by S008, which uses pypdf/pdfinfo and verifies the generated
  PAGE01 PDFs/SVGs are non-empty text output without table-grid promotion
  markers.
- A001 failed because an intentionally broad grep matched non-production
  evidence/probe paths. Superseded by A001R, which checks added production diff
  lines for PAGE01/corpus/reference-PDF hardcoding.
- A002R failed because it expected safety guard strings to be newly added in the
  diff. Superseded by A002S and S004, because the correct criterion is that the
  existing safety guard is still active and the artifacts remain non-promoted.
- A003 failed because the process grep matched its own running shell command.
  Superseded by A003R, which excludes the current shell/pgrep and passes.

Post-review code-quality blocker fixes:
- probe_signals.rs was split into responsibility-focused submodules; all probe
  source/test modules are now below 250 lines in the final-review-fixes audit.
- source_y_probe tests are marked ignored/manual by default and fail loudly if
  explicitly run without the local corpus.
- document_text_control gap heuristic now has boundary and negative tests for
  the configured three-empty-row limit.

Final verdict: resolved-pass. Use manualQa-matrix.json plus
final-review-fixes.txt as the current gate evidence.
